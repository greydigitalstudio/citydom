import React from 'react';
import styles from '../../page.module.css'

const Filter = () => {
    return (
        <div className={styles.filter}>
            <div className={styles.filter__in}></div>
        </div>
    );
}

export default Filter;
